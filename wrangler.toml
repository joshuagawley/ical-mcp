name = "ical-mcp"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[durable_objects]
bindings = [
  { name = "MCP_SERVER", class_name = "McpServer" }
]

[[migrations]]
tag = "v1"
new_classes = ["McpServer"]

# Secrets (set via `bunx wrangler secret put`):
# - APPLE_ID
# - APP_PASSWORD
